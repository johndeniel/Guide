# Guide

<div align="justify"> This repository serves as a comprehensive guide for various commands and resources related to Git, Docker and more. Whether you're new to these technologies or looking for quick references, this guide can help you get started or serve as a handy reference. </div>

## Table of Contents

- [Git](#git)
- [Docker](#docker)

## Git

<div align="justify"> Git is a popular distributed version control system. It allows multiple developers to collaborate on a project and efficiently manage changes to source code. Here are some essential Git commands: </div>

<br> 

- `git init`: Initialize a new Git repository.
- `git clone <repository-url>`: Clone a remote Git repository to your local machine.
- `git add <file>`: Stage a file for commit.
- `git commit -m "Commit message"`: Create a new commit with staged changes.
- `git push`: Push commits to a remote repository.
- `git pull`: Fetch changes from a remote repository and merge them into your local branch.
- `git branch`: List, create, or delete branches.
- `git checkout <branch-name>`: Switch to a different branch.
- `git merge <branch-name>`: Merge changes from a different branch into the current branch.

## Docker

<div align="justify"> Docker is a platform that allows you to build, distribute, and run applications using containerization. It provides an efficient and consistent environment for software deployment. Here are some useful Docker commands: </div>

<br> 

- `docker build -t <image-name> .`: Build a Docker image from a Dockerfile.
- `docker run <image-name>`: Run a Docker container based on an image.
- `docker ps`: List running containers.
- `docker stop <container-id>`: Stop a running container.
- `docker images`: List available Docker images.
- `docker pull <image-name>`: Pull a Docker image from a registry.
- `docker push <image-name>`: Push a Docker image to a registry.


## Additional Resources

- [Git Documentation](https://git-scm.com/doc)
- [Docker Documentation](https://docs.docker.com/)

Feel free to explore these resources for more in-depth information and examples.

---

<div align="justify"> This guide is just a starting point, and there are many more commands and concepts to explore. It's recommended to refer to official documentation and specific resource repositories for more detailed information on each technology. </div>
